h1 Watcha
if search
    p #{search} 에 대한 검색결과 입니다
p #{news.length}개의 영화 뉴스 정보가 표시됩니다


for newsInformation in news
    img(src=newsInformation.image, width='200', height='200')

    h2 #{newsInformation.title}
    p #{newsInformation.content}